export enum BonesRevealState {
    PENDING_REVEAL = "PENDING_REVEAL", 
    PENDING_REGISTERING = "PENDING_REGISTERING" ,
    NA="NA", 
    DONE = "DONE",

    
}
// function getMedia(state: string): BonesRevealState {
//     if (  state === 'Forbes' ) {
//         return BonesRevealState.Magazine;
//     }else if (  state === 'Forbes'  ){

//     }else if (  state === 'Forbes'  ){
        
//     }else if (  state === 'Forbes'  ){
        
//     }
//  }



